// import { selectSinglePostPageDomain } from '../selectors';

describe('selectSinglePostPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
