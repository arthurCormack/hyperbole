// import { selectSinglePostPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
// import expect from 'expect';

// const selector = selectSinglePostPageDomain();

describe('selectArchivePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
