// import expect from 'expect';
import singlePostPageReducer from '../reducer';
import { fromJS } from 'immutable';

describe('archivePageReducer', () => {
  it('returns the initial state', () => {
    expect(true).toEqual(true);
  });
});
